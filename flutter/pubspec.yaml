name: etotop_client
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_bloc:
  get_it:
  shared_preferences: 
  flutter_secure_storage:
  go_router:
  intl: 0.20.2
  cupertino_icons: ^1.0.9
  rust_lib_etotop_client:
    path: rust_builder
  flutter_rust_bridge: 2.12.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter

flutter:
  generate: true
  uses-material-design: true
